name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GOWORK: off
      GOPROXY: direct
      GOSUMDB: off
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Download dependencies and run tests
        run: |
          for dir in services/*/; do
            if [ -f "$dir/go.mod" ]; then
              name=$(basename "$dir")
              echo "=== Testing $name ==="
              (cd "$dir" && go mod download && go test -race -coverprofile=coverage.txt -covermode=atomic ./...) || echo "Tests failed for $name"
            fi
          done

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          verbose: true
